<%= form_with(model: school, local: true) do |form| %>
  <% if school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
      <% school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codschool %>
    <%= form.text_field :codschool, id: :school_codschool %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :school_nombre %>
  </div>

  <div class="field">
    <%= form.label :id_teacher_id %>
    <%= form.text_field :id_teacher_id, id: :school_id_teacher_id %>
  </div>

  <div class="field">
    <%= form.label :id_subject_id %>
    <%= form.text_field :id_subject_id, id: :school_id_subject_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
