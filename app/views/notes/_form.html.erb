<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :note %>
    <%= form.number_field :note, id: :note_note %>
  </div>

  <div class="field">
    <%= form.label :id_student_id %>
    <%= form.text_field :id_student_id, id: :note_id_student_id %>
  </div>

  <div class="field">
    <%= form.label :id_group_id %>
    <%= form.text_field :id_group_id, id: :note_id_group_id %>
  </div>

  <div class="field">
    <%= form.label :id_subject_id %>
    <%= form.text_field :id_subject_id, id: :note_id_subject_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
