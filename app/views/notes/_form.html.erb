<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor, id: :note_valor %>
  </div>

  <div class="field">
    <%= form.label :semester_id %>
    <%= form.text_field :semester_id, id: :note_semester_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
