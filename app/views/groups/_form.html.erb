<%= form_with(model: group, local: true) do |form| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codgroup %>
    <%= form.text_field :codgroup, id: :group_codgroup %>
  </div>

  <div class="field">
    <%= form.label :id_subject_id %>
    <%= form.text_field :id_subject_id, id: :group_id_subject_id %>
  </div>

  <div class="field">
    <%= form.label :id_teacher_id %>
    <%= form.text_field :id_teacher_id, id: :group_id_teacher_id %>
  </div>

  <div class="field">
    <%= form.label :id_student_id %>
    <%= form.text_field :id_student_id, id: :group_id_student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
